<div class="row align-items-center g-lg-5 py-5">
    <%= bootstrap_form_with url: sign_in_path, local: true do |f| %>
    <%= f.email_field :email %>
    <%= f.password_field :password %>
    <%= f.check_box :remember_me %>
    <%= f.submit "Sign In", class: "btn btn-primary"%>
    <% end %>
</div>